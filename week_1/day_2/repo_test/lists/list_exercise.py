
# 1. Create an empty list called `task_list`
task_list = []

# 2. Add a few `str` elements, representing some everyday tasks e.g. 'Make Dinner'
task_list = ['sleep', 'eat', 'shower', 'clean', 'dance',]
task_list.append("brush teeth")
task_list.append("watch tv")
# 3. Print out `task_list`
print(task_list)
# 4. Remove the last task
task_list.pop()
# 5. Print out `task_list`
print(task_list)
# 6. Print out the number of elements in `task_list`
print(len(task_list))


#taskList1.extend(taskList2)
#taskList1.extend(['item1', 'item2'])

#mutable = can be changed eg list
#inmutable = can not be changed eg tuple good for data purposes assigned amount of space